repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
        description: "Remove trailing whitespace from lines"
      - id: end-of-file-fixer
        description: "Ensure files end with a newline"
      - id: check-yaml
        description: "Check that YAML files are valid"
      - id: double-quote-string-fixer
        description: "Fix double quotes in strings"
      - id: requirements-txt-fixer
        description: "Fix issues in requirements.txt files"
      - id: check-ast
        description: "Check Python files for syntax errors"
      - id: check-xml
        description: "Validate XML files"
      - id: check-json
        description: "Validate JSON files"
      - id: check-merge-conflict
        description: "Detect unresolved merge conflicts"

  # - repo: https://github.com/psf/black
  #   rev: 23.1.0
  #   hooks:
  #     - id: black
  #       language_version: python3
  #       description: "Automatically format Python code using black"

  # - repo: https://github.com/PyCQA/pydocstyle
  #   rev: 6.3.0
  #   hooks:
  #     - id: pydocstyle
  #       args:
  #         - "--add-ignore=D100" # Ignore missing module-level docstrings
  #         - "--add-ignore=D101" # Ignore missing docstrings in classes
  #         - "--add-ignore=D102" # Ignore missing docstrings in public methods
  #         - "--add-ignore=D104" # Ignore missing docstrings in private methods
  #         - "--add-ignore=D200" # Ignore one-line docstring length checks
  #         - "--add-ignore=D202" # Ignore blank line after docstring
  #         - "--add-ignore=D204" # Ignore blank line after class docstring
  #         - "--add-ignore=D205" # Ignore blank line between summary and description
  #         - "--add-ignore=D400" # Ignore ending punctuation
  #         - "--add-ignore=D401" # Ignore first line imperative mood
  #       description: "Relaxed docstring enforcement"

  # - repo: https://github.com/pre-commit/mirrors-autopep8
  #   rev: v1.7.0
  #   hooks:
  #     - id: autopep8
  #       args: ["--in-place", "--aggressive", "--recursive"]
  #       description: "Automatically fix Python code style"
