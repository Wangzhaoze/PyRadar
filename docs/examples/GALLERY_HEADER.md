# Examples

The executable examples below use deterministic synthetic ADC and point-cloud
data. The adjacent command-line scripts demonstrate the same APIs on local
ColoRadar, RaDelft, and RAMPCNN captures without executing during the docs build.
